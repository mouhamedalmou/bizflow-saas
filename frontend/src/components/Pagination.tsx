import { ChevronLeft, ChevronRight } from "lucide-react";
export interface PaginationProps { currentPage: number; totalPages: number; onPageChange: (page: number) => void; siblingCount?: number }
export function Pagination({ currentPage, totalPages, onPageChange, siblingCount = 1 }: PaginationProps) { if (totalPages <= 1) return null; const start = Math.max(1, currentPage - siblingCount); const end = Math.min(totalPages, currentPage + siblingCount); const pages = Array.from({ length: end - start + 1 }, (_, index) => start + index); return <nav aria-label="Paginazione" className="flex items-center justify-center gap-1"><button aria-label="Pagina precedente" disabled={currentPage <= 1} onClick={() => onPageChange(currentPage - 1)} className="rounded-lg p-2 text-slate-300 outline-none hover:bg-slate-800 focus-visible:ring-2 focus-visible:ring-indigo-400 disabled:opacity-40"><ChevronLeft size={18} /></button>{start > 1 && <><button onClick={() => onPageChange(1)} className="h-9 min-w-9 rounded-lg text-sm text-slate-300 hover:bg-slate-800">1</button>{start > 2 && <span className="px-1 text-slate-500">…</span>}</>}{pages.map((page) => <button key={page} aria-current={page === currentPage ? "page" : undefined} onClick={() => onPageChange(page)} className={`h-9 min-w-9 rounded-lg px-2 text-sm font-medium outline-none focus-visible:ring-2 focus-visible:ring-indigo-400 ${page === currentPage ? "bg-indigo-600 text-white" : "text-slate-300 hover:bg-slate-800"}`}>{page}</button>)}{end < totalPages && <>{end < totalPages - 1 && <span className="px-1 text-slate-500">…</span>}<button onClick={() => onPageChange(totalPages)} className="h-9 min-w-9 rounded-lg text-sm text-slate-300 hover:bg-slate-800">{totalPages}</button></>}<button aria-label="Pagina successiva" disabled={currentPage >= totalPages} onClick={() => onPageChange(currentPage + 1)} className="rounded-lg p-2 text-slate-300 outline-none hover:bg-slate-800 focus-visible:ring-2 focus-visible:ring-indigo-400 disabled:opacity-40"><ChevronRight size={18} /></button></nav>; }
