/* eslint-disable react-refresh/only-export-components -- compatibility barrel, not a refresh boundary */
/** @deprecated Import from `components` or the individual component module. */
export * from "./index";
