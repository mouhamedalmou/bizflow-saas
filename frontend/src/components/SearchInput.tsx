import { useEffect, useId, useState } from "react";
import { Filter, Search, X } from "lucide-react";
export interface SearchInputProps { placeholder?: string; onChange: (value: string) => void; debounceMs?: number; initialValue?: string; label?: string }
export function SearchInput({ placeholder = "Cerca...", onChange, debounceMs = 300, initialValue = "", label = "Cerca" }: SearchInputProps) { const [value, setValue] = useState(initialValue); useEffect(() => { const timer = window.setTimeout(() => onChange(value), debounceMs); return () => window.clearTimeout(timer); }, [debounceMs, onChange, value]); return <label className="relative block w-full sm:min-w-72"><span className="sr-only">{label}</span><Search aria-hidden="true" className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" size={18} /><input type="search" value={value} onChange={(event) => setValue(event.target.value)} placeholder={placeholder} className="w-full rounded-lg border border-slate-300 bg-white py-2.5 pl-10 pr-10 text-sm text-slate-950 outline-none transition-all duration-200 placeholder:text-slate-400 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-500/20 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100 dark:placeholder:text-slate-500" />{value && <button aria-label="Cancella ricerca" type="button" onClick={() => setValue("")} className="absolute right-2 top-1/2 -translate-y-1/2 rounded p-1 text-slate-500 hover:bg-slate-100 hover:text-slate-950 dark:hover:bg-slate-800 dark:hover:text-white"><X size={16} /></button>}</label>; }
export interface FilterOption { value: string; label: string }
export interface FilterDropdownProps { options: FilterOption[]; value: string[]; onChange: (values: string[]) => void; label?: string }
export function FilterDropdown({ options, value, onChange, label = "Filtri" }: FilterDropdownProps) { const id = useId(); const toggle = (option: string) => onChange(value.includes(option) ? value.filter((item) => item !== option) : [...value, option]); return <details className="relative"><summary className="flex cursor-pointer list-none items-center gap-2 rounded-lg border border-slate-700 bg-slate-900 px-3 py-2.5 text-sm text-slate-200 outline-none hover:border-slate-600 focus-visible:ring-2 focus-visible:ring-indigo-400"><Filter size={17} />{label}{value.length > 0 && <span className="rounded-full bg-indigo-600 px-1.5 text-xs">{value.length}</span>}</summary><div className="absolute right-0 z-20 mt-2 min-w-56 rounded-lg border border-slate-700 bg-slate-900 p-2 shadow-xl">{options.map((option) => <label key={option.value} htmlFor={`${id}-${option.value}`} className="flex cursor-pointer items-center gap-2 rounded-lg px-3 py-2 text-sm text-slate-200 hover:bg-slate-800"><input id={`${id}-${option.value}`} type="checkbox" checked={value.includes(option.value)} onChange={() => toggle(option.value)} className="h-4 w-4 rounded border-slate-600 bg-slate-950 accent-indigo-600" />{option.label}</label>)}</div></details>; }
