import { useState } from "react";
import { ChevronLeft, ChevronRight, ShoppingCart } from "lucide-react";
import { Button } from "./Button";
import { Badge } from "./Badge";
export interface ProductCardProduct { id: string; name: string; price: number; image?: string; imageUrl?: string; images?: string[]; stock: number }
export interface ProductCardProps { product: ProductCardProduct; onAddToCart?: (product: ProductCardProduct) => void; loading?: boolean }
export function ProductCard({ product, onAddToCart, loading }: ProductCardProps) { const images = product.images?.length ? product.images : [product.imageUrl ?? product.image ?? ""]; const [index, setIndex] = useState(0); const inStock = product.stock > 0; return <article className="group overflow-hidden rounded-lg border border-slate-700 bg-slate-900 transition-all duration-200 hover:-translate-y-0.5 hover:border-slate-600 hover:shadow-lg hover:shadow-indigo-950/20"><div className="relative aspect-[4/3] overflow-hidden bg-slate-800"><img src={images[index]} alt={`${product.name}, immagine ${index + 1}`} className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" />{images.length > 1 && <><button aria-label="Immagine precedente" onClick={() => setIndex((value) => (value - 1 + images.length) % images.length)} className="absolute left-2 top-1/2 -translate-y-1/2 rounded-full bg-slate-950/70 p-1.5 text-white"><ChevronLeft size={17} /></button><button aria-label="Immagine successiva" onClick={() => setIndex((value) => (value + 1) % images.length)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-full bg-slate-950/70 p-1.5 text-white"><ChevronRight size={17} /></button></>}</div><div className="p-4"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold text-slate-100">{product.name}</h3><Badge label={inStock ? `${product.stock} disponibili` : "Esaurito"} color={inStock ? product.stock < 10 ? "amber" : "emerald" : "red"} /></div><p className="mt-3 text-xl font-bold text-indigo-400">{new Intl.NumberFormat("it-IT", { style: "currency", currency: "EUR" }).format(product.price)}</p><Button fullWidth className="mt-4" icon={<ShoppingCart size={17} />} disabled={!inStock} loading={loading} onClick={() => onAddToCart?.(product)}>Aggiungi al carrello</Button></div></article>; }
