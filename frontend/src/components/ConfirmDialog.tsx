import { AlertTriangle } from "lucide-react";
import { Button } from "./Button";
import { Modal } from "./Modal";
export interface ConfirmDialogProps { isOpen: boolean; title: string; message: string; onConfirm: () => void | Promise<void>; onCancel: () => void; isDangerous?: boolean; loading?: boolean; confirmLabel?: string; cancelLabel?: string }
export function ConfirmDialog({ isOpen, title, message, onConfirm, onCancel, isDangerous = false, loading, confirmLabel = "Conferma", cancelLabel = "Annulla" }: ConfirmDialogProps) { return <Modal isOpen={isOpen} onClose={onCancel} title={title} size="sm" footer={<div className="flex justify-end gap-2"><Button variant="ghost" onClick={onCancel}>{cancelLabel}</Button><Button variant={isDangerous ? "danger" : "primary"} loading={loading} onClick={() => void onConfirm()}>{confirmLabel}</Button></div>}><div className="flex gap-3"><span className={`shrink-0 rounded-full p-2 ${isDangerous ? "bg-red-500/15 text-red-400" : "bg-amber-500/15 text-amber-400"}`}><AlertTriangle /></span><p className="text-sm leading-6 text-slate-300">{message}</p></div></Modal>; }
