import type { HTMLAttributes, ReactNode } from "react";
export interface CardProps extends Omit<HTMLAttributes<HTMLElement>, "title"> { title?: ReactNode; subtitle?: ReactNode; footer?: ReactNode; interactive?: boolean; elevation?: "none" | "sm" | "md"; hover?: boolean; children: ReactNode }
export function Card({ title, subtitle, footer, interactive = false, elevation = "none", hover = true, children, className = "", ...props }: CardProps) { const shadows = { none: "", sm: "shadow-sm shadow-slate-900/8 dark:shadow-black/20", md: "shadow-lg shadow-slate-900/10 dark:shadow-black/25" }; const effects = interactive || hover ? "transition-all duration-200 ease-in-out hover:border-slate-300 dark:hover:border-slate-600" : ""; return <article className={`overflow-hidden rounded-lg border border-slate-200 bg-white text-slate-900 dark:border-slate-700 dark:bg-slate-900/90 dark:text-slate-100 ${shadows[elevation]} ${effects} ${interactive ? "cursor-pointer hover:-translate-y-0.5 hover:shadow-lg hover:shadow-indigo-500/10 dark:hover:shadow-indigo-950/20" : ""} ${className}`} {...props}>{(title || subtitle) && <header className="border-b border-slate-200 px-4 py-4 dark:border-slate-700"><h3 className="font-semibold text-slate-900 dark:text-slate-100">{title}</h3>{subtitle && <p className="mt-1 text-sm text-slate-600 dark:text-slate-400">{subtitle}</p>}</header>}<div className="p-4">{children}</div>{footer && <footer className="border-t border-slate-200 bg-slate-50 px-4 py-3 dark:border-slate-700 dark:bg-slate-950/30">{footer}</footer>}</article>; }
