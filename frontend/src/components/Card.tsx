import type { HTMLAttributes, ReactNode } from "react";
export interface CardProps extends Omit<HTMLAttributes<HTMLElement>, "title"> { title?: ReactNode; subtitle?: ReactNode; footer?: ReactNode; interactive?: boolean; elevation?: "none" | "sm" | "md"; hover?: boolean; children: ReactNode }
export function Card({ title, subtitle, footer, interactive = false, elevation = "none", hover = true, children, className = "", ...props }: CardProps) { const shadows = { none: "", sm: "shadow-sm shadow-black/20", md: "shadow-lg shadow-black/25" }; const effects = interactive || hover ? "transition-all duration-200 ease-in-out hover:border-slate-600" : ""; return <article className={`overflow-hidden rounded-lg border border-slate-700 bg-slate-900/90 ${shadows[elevation]} ${effects} ${interactive ? "cursor-pointer hover:-translate-y-0.5 hover:shadow-lg hover:shadow-indigo-950/20" : ""} ${className}`} {...props}>{(title || subtitle) && <header className="border-b border-slate-700 px-4 py-4"><h3 className="font-semibold text-slate-100">{title}</h3>{subtitle && <p className="mt-1 text-sm text-slate-400">{subtitle}</p>}</header>}<div className="p-4">{children}</div>{footer && <footer className="border-t border-slate-700 bg-slate-950/30 px-4 py-3">{footer}</footer>}</article>; }
