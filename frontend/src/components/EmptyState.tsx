import type { ReactNode } from "react";
import { AlertTriangle, FileQuestion, PackageOpen } from "lucide-react";
import { Button } from "./Button";
interface StateProps { icon?: ReactNode; title?: string; message: string; actionLabel?: string; onAction?: () => void }
export function EmptyState({ icon = <PackageOpen />, title = "Nessun risultato", message, actionLabel, onAction }: StateProps) { return <section className="grid place-items-center rounded-lg border border-dashed border-slate-700 p-8 text-center"><span className="rounded-full bg-slate-800 p-3 text-slate-400">{icon}</span><h2 className="mt-3 font-semibold text-slate-100">{title}</h2><p className="mt-1 max-w-md text-sm text-slate-400">{message}</p>{actionLabel && <Button className="mt-4" onClick={onAction}>{actionLabel}</Button>}</section>; }
export function ErrorState({ message, retryLabel = "Riprova", onRetry }: { message: string; retryLabel?: string; onRetry?: () => void }) { return <section role="alert" className="grid place-items-center rounded-lg border border-red-500/30 bg-red-500/10 p-8 text-center"><AlertTriangle className="text-red-400" /><h2 className="mt-3 font-semibold text-red-300">Qualcosa è andato storto</h2><p className="mt-1 text-sm text-red-200/80">{message}</p>{onRetry && <Button variant="danger" className="mt-4" onClick={onRetry}>{retryLabel}</Button>}</section>; }
export function NotFoundPage({ onBack }: { onBack?: () => void }) { return <main className="grid min-h-[70vh] place-items-center px-4 text-center"><div><FileQuestion className="mx-auto text-indigo-400" size={56} /><p className="mt-4 text-7xl font-black text-slate-700">404</p><h1 className="mt-2 text-2xl font-bold text-slate-100">Pagina non trovata</h1><p className="mt-2 text-slate-400">La pagina richiesta non esiste o è stata spostata.</p><Button className="mt-6" onClick={onBack ?? (() => history.back())}>Torna indietro</Button></div></main>; }
