import { Eye, XCircle } from "lucide-react";
import { Button } from "./Button";
import { Card } from "./Card";
import { StatusBadge, type DisplayOrderStatus } from "./StatusBadge";
export interface OrderCardOrder { id: string; date: string | Date; status: DisplayOrderStatus; totalPrice: number; itemCount: number }
export interface OrderCardProps { order: OrderCardOrder; onView?: (order: OrderCardOrder) => void; onCancel?: (order: OrderCardOrder) => void }
export function OrderCard({ order, onView, onCancel }: OrderCardProps) { const cancellable = order.status === "pending" || order.status === "processing"; return <Card hover interactive={false} footer={<div className="flex flex-wrap justify-end gap-2"><Button variant="ghost" size="sm" icon={<Eye size={16} />} onClick={() => onView?.(order)}>Dettagli</Button>{cancellable && <Button variant="danger" size="sm" icon={<XCircle size={16} />} onClick={() => onCancel?.(order)}>Annulla</Button>}</div>}><div className="flex items-start justify-between gap-4"><div><p className="font-semibold text-slate-100">Ordine #{order.id}</p><time className="text-xs text-slate-400" dateTime={new Date(order.date).toISOString()}>{new Intl.DateTimeFormat("it-IT", { dateStyle: "medium" }).format(new Date(order.date))}</time></div><StatusBadge status={order.status} /></div><div className="mt-4 grid grid-cols-2 gap-4"><div><p className="text-xs text-slate-500">Articoli</p><p className="font-semibold text-slate-200">{order.itemCount}</p></div><div className="text-right"><p className="text-xs text-slate-500">Totale</p><p className="font-bold text-indigo-400">{new Intl.NumberFormat("it-IT", { style: "currency", currency: "EUR" }).format(order.totalPrice)}</p></div></div></Card>; }
