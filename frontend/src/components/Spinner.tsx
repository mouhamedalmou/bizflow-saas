import { LoaderCircle } from "lucide-react";
export function LoadingSpinner({ label = "Caricamento...", size = "md" }: { label?: string; size?: "sm" | "md" | "lg" }) { const pixels = { sm: 16, md: 24, lg: 36 }; return <div role="status" className="inline-flex items-center gap-2 text-sm text-slate-400"><LoaderCircle aria-hidden="true" className="animate-spin text-indigo-500" size={pixels[size]} /><span>{label}</span></div>; }
export function Skeleton({ className = "h-4 w-full", rounded = "lg" }: { className?: string; rounded?: "sm" | "md" | "lg" | "full" }) { const radii = { sm: "rounded-sm", md: "rounded-md", lg: "rounded-lg", full: "rounded-full" }; return <span aria-hidden="true" className={`block animate-pulse bg-slate-800 ${radii[rounded]} ${className}`} />; }
export function ProgressBar({ value, label }: { value: number; label?: string }) { const progress = Math.min(100, Math.max(0, value)); return <div className="w-full"><div className="mb-1 flex justify-between text-xs text-slate-400"><span>{label}</span><span>{Math.round(progress)}%</span></div><div role="progressbar" aria-label={label ?? "Avanzamento"} aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100} className="h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-indigo-600 transition-[width] duration-300" style={{ width: `${progress}%` }} /></div></div>; }
